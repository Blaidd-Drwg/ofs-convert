language: cpp
sudo: required

services:
  - docker

before_install:
  - sudo apt-get -qq update
  - sudo apt-get -y install realpath

script:
  - cd test/container
  - ./build.sh
  - OFS_CONVERT_TOOL_TIMEOUT=60 ./run.sh
